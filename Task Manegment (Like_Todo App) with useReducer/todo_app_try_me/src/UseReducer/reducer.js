export const initialState = {
    todos: [
        {
            id: 11,
            name: 'Din islam'
        }
    ]
}

export const reducer = (state, action) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}